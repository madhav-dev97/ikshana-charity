kind = "web"
previewPath = "/"
title = "IKSHANA CHARITABLE TRUST"
version = "1.0.0"
id = "artifacts/trust-giving"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20038

[services.development]
run = "pnpm --filter @workspace/trust-giving run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trust-giving", "run", "build" ]
publicDir = "artifacts/trust-giving/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20038"
BASE_PATH = "/"
